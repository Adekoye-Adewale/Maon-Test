/** @type {import('next').NextConfig} */
const nextConfig = {
        images: {
                domains: [
                        'static.seekingalpha.com', 
                        's.yimg.com',
                        'image.cnbcfm.com',
                        'static2.finnhub.io',
                        'data.bloomberglp.com'
                ]
        }
};

export default nextConfig;
